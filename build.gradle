buildscript {
    repositories {
        jcenter()
        mavenCentral()
    }
    dependencies {
        classpath(group: 'com.moowork.gradle', name: 'gradle-node-plugin', version: '0.12')
    }
}

allprojects {
    apply plugin: 'maven'
}
